---
title: 'gRPC API'
description: 'High-performance gRPC interface for programmatic access'
---
gRPC provides the highest performance access to ansemchain with strongly-typed clients.

## Endpoint

```
localhost:9090
```

For TLS-enabled endpoints (testnet/mainnet):
```
grpc.ansem.network:443
```

## Prerequisites

### grpcurl

Install grpcurl for command-line testing:

```bash wrap
# macOS
brew install grpcurl

# Linux
go install github.com/fullstorydev/grpcurl/cmd/grpcurl@latest
```

### Reflection

ansemchain supports gRPC reflection, allowing clients to discover services:

```bash wrap
grpcurl -plaintext localhost:9090 list
```

## Service Discovery

### List All Services

```bash wrap
grpcurl -plaintext localhost:9090 list
```

<Accordion title="Output">
```
cosmos.auth.v1beta1.Query
cosmos.bank.v1beta1.Query
cosmos.distribution.v1beta1.Query
cosmos.gov.v1.Query
cosmos.mint.v1beta1.Query
cosmos.staking.v1beta1.Query
cosmos.tx.v1beta1.Service
cosmwasm.wasm.v1.Query
grpc.reflection.v1alpha.ServerReflection
```
</Accordion>

### List Methods in Service

```bash wrap
grpcurl -plaintext localhost:9090 list cosmos.bank.v1beta1.Query
```

Output:
```
cosmos.bank.v1beta1.Query.AllBalances
cosmos.bank.v1beta1.Query.Balance
cosmos.bank.v1beta1.Query.DenomMetadata
cosmos.bank.v1beta1.Query.DenomsMetadata
cosmos.bank.v1beta1.Query.Params
cosmos.bank.v1beta1.Query.SpendableBalances
cosmos.bank.v1beta1.Query.SupplyOf
cosmos.bank.v1beta1.Query.TotalSupply
```

### Describe Method

```bash wrap
grpcurl -plaintext localhost:9090 describe cosmos.bank.v1beta1.Query.AllBalances
```

## Bank Queries

### All Balances

```bash wrap
grpcurl -plaintext \
  -d '{"address": "ansem1address..."}' \
  localhost:9090 \
  cosmos.bank.v1beta1.Query/AllBalances
```

<Accordion title="Response">
```json wrap
{
  "balances": [
    {"denom": "uchanse", "amount": "1000000"}
  ],
  "pagination": {"total": "1"}
}
```
</Accordion>

### Single Balance

```bash wrap
grpcurl -plaintext \
  -d '{"address": "ansem1address...", "denom": "uchanse"}' \
  localhost:9090 \
  cosmos.bank.v1beta1.Query/Balance
```

### Total Supply

```bash wrap
grpcurl -plaintext localhost:9090 cosmos.bank.v1beta1.Query/TotalSupply
```

### Spendable Balances

```bash wrap
grpcurl -plaintext \
  -d '{"address": "ansem1address..."}' \
  localhost:9090 \
  cosmos.bank.v1beta1.Query/SpendableBalances
```

## Auth Queries

### Get Account

```bash wrap
grpcurl -plaintext \
  -d '{"address": "ansem1address..."}' \
  localhost:9090 \
  cosmos.auth.v1beta1.Query/Account
```

<Accordion title="Response">
```json wrap
{
  "account": {
    "@type": "/cosmos.auth.v1beta1.BaseAccount",
    "address": "ansem1address...",
    "pubKey": {
      "@type": "/cosmos.crypto.secp256k1.PubKey",
      "key": "A..."
    },
    "accountNumber": "0",
    "sequence": "5"
  }
}
```
</Accordion>

### List Accounts

```bash wrap
grpcurl -plaintext localhost:9090 cosmos.auth.v1beta1.Query/Accounts
```

## Staking Queries

### Get Validators

```bash wrap
grpcurl -plaintext localhost:9090 cosmos.staking.v1beta1.Query/Validators
```

### Get Delegations

```bash wrap
grpcurl -plaintext \
  -d '{"delegator_addr": "ansem1delegator..."}' \
  localhost:9090 \
  cosmos.staking.v1beta1.Query/DelegatorDelegations
```

### Get Staking Pool

```bash wrap
grpcurl -plaintext localhost:9090 cosmos.staking.v1beta1.Query/Pool
```

## CosmWasm Queries

### List Codes

```bash wrap
grpcurl -plaintext localhost:9090 cosmwasm.wasm.v1.Query/Codes
```

<Accordion title="Response">
```json wrap
{
  "codeInfos": [
    {
      "codeId": "1",
      "creator": "ansem1creator...",
      "dataHash": "ABC123...",
      "instantiatePermission": {"permission": "EVERYBODY"}
    }
  ]
}
```
</Accordion>

### Get Code Info

```bash wrap
grpcurl -plaintext \
  -d '{"code_id": "1"}' \
  localhost:9090 \
  cosmwasm.wasm.v1.Query/Code
```

### List Contracts by Code

```bash wrap
grpcurl -plaintext \
  -d '{"code_id": "1"}' \
  localhost:9090 \
  cosmwasm.wasm.v1.Query/ContractsByCode
```

### Get Contract Info

```bash wrap
grpcurl -plaintext \
  -d '{"address": "ansem1contract..."}' \
  localhost:9090 \
  cosmwasm.wasm.v1.Query/ContractInfo
```

### Query Smart Contract

Query message must be base64-encoded:

```bash wrap
# Encode query
QUERY=$(echo -n '{"get_count":{}}' | base64)

grpcurl -plaintext \
  -d "{\"address\": \"ansem1contract...\", \"query_data\": \"$QUERY\"}" \
  localhost:9090 \
  cosmwasm.wasm.v1.Query/SmartContractState
```

<Accordion title="Response">
```json wrap
{
  "data": "eyJjb3VudCI6NDJ9"
}
```
The data is base64-encoded JSON. Decode: `{"count":42}`
</Accordion>

### Get Contract History

```bash wrap
grpcurl -plaintext \
  -d '{"address": "ansem1contract..."}' \
  localhost:9090 \
  cosmwasm.wasm.v1.Query/ContractHistory
```

## Transaction Service

### Simulate Transaction

```bash wrap
grpcurl -plaintext \
  -d '{"tx_bytes": "base64-encoded-tx"}' \
  localhost:9090 \
  cosmos.tx.v1beta1.Service/Simulate
```

### Broadcast Transaction

```bash wrap
grpcurl -plaintext \
  -d '{"tx_bytes": "base64-encoded-signed-tx", "mode": "BROADCAST_MODE_SYNC"}' \
  localhost:9090 \
  cosmos.tx.v1beta1.Service/BroadcastTx
```

### Get Transaction

```bash wrap
grpcurl -plaintext \
  -d '{"hash": "ABC123..."}' \
  localhost:9090 \
  cosmos.tx.v1beta1.Service/GetTx
```

### Search Transactions

```bash wrap
grpcurl -plaintext \
  -d '{"events": ["message.sender='"'"'ansem1sender...'"'"'"]}' \
  localhost:9090 \
  cosmos.tx.v1beta1.Service/GetTxsEvent
```

## Node Service

### Get Node Info

```bash wrap
grpcurl -plaintext localhost:9090 cosmos.base.tendermint.v1beta1.Service/GetNodeInfo
```

### Get Syncing Status

```bash wrap
grpcurl -plaintext localhost:9090 cosmos.base.tendermint.v1beta1.Service/GetSyncing
```

### Get Latest Block

```bash wrap
grpcurl -plaintext localhost:9090 cosmos.base.tendermint.v1beta1.Service/GetLatestBlock
```

### Get Block by Height

```bash wrap
grpcurl -plaintext \
  -d '{"height": "1000"}' \
  localhost:9090 \
  cosmos.base.tendermint.v1beta1.Service/GetBlockByHeight
```

## Using with Go

### Setup Client

```go wrap
package main

import (
    "context"
    "fmt"
    "google.golang.org/grpc"
    "google.golang.org/grpc/credentials/insecure"

    banktypes "github.com/cosmos/cosmos-sdk/x/bank/types"
)

func main() {
    // Connect to gRPC
    conn, err := grpc.Dial(
        "localhost:9090",
        grpc.WithTransportCredentials(insecure.NewCredentials()),
    )
    if err != nil {
        panic(err)
    }
    defer conn.Close()

    // Create bank query client
    bankClient := banktypes.NewQueryClient(conn)

    // Query balance
    res, err := bankClient.AllBalances(context.Background(), &banktypes.QueryAllBalancesRequest{
        Address: "ansem1address...",
    })
    if err != nil {
        panic(err)
    }

    fmt.Printf("Balances: %v\n", res.Balances)
}
```

### Query CosmWasm Contract

```go wrap
package main

import (
    "context"
    "encoding/base64"
    "encoding/json"
    "fmt"

    wasmtypes "github.com/CosmWasm/wasmd/x/wasm/types"
    "google.golang.org/grpc"
    "google.golang.org/grpc/credentials/insecure"
)

func main() {
    conn, _ := grpc.Dial("localhost:9090", grpc.WithTransportCredentials(insecure.NewCredentials()))
    defer conn.Close()

    wasmClient := wasmtypes.NewQueryClient(conn)

    // Prepare query
    queryMsg := map[string]interface{}{"get_count": map[string]interface{}{}}
    queryBytes, _ := json.Marshal(queryMsg)

    // Query contract
    res, _ := wasmClient.SmartContractState(context.Background(), &wasmtypes.QuerySmartContractStateRequest{
        Address:   "ansem1contract...",
        QueryData: queryBytes,
    })

    // Parse response
    var result map[string]interface{}
    json.Unmarshal(res.Data, &result)
    fmt.Printf("Result: %v\n", result)
}
```

## Using with JavaScript

### Setup with CosmJS

```javascript wrap
import { QueryClient, createProtobufRpcClient } from "@cosmjs/stargate";
import { Tendermint37Client } from "@cosmjs/tendermint-rpc";
import { QueryClientImpl as BankQuery } from "cosmjs-types/cosmos/bank/v1beta1/query";

async function main() {
  // Connect via Chain RPC (which proxies to gRPC internally)
  const tendermint = await Tendermint37Client.connect("https://rpc.ansemchain.fun");
  const queryClient = QueryClient.withExtensions(tendermint);
  const rpc = createProtobufRpcClient(queryClient);

  // Create bank query client
  const bankQuery = new BankQuery(rpc);

  // Query balance
  const balance = await bankQuery.AllBalances({
    address: "ansem1address..."
  });

  console.log("Balances:", balance.balances);
}

main();
```

## Pagination

Most list queries support pagination:

```bash wrap
# First page
grpcurl -plaintext \
  -d '{"pagination": {"limit": "10"}}' \
  localhost:9090 \
  cosmos.bank.v1beta1.Query/TotalSupply

# Next page
grpcurl -plaintext \
  -d '{"pagination": {"key": "base64-next-key", "limit": "10"}}' \
  localhost:9090 \
  cosmos.bank.v1beta1.Query/TotalSupply
```

## TLS Connection

For testnet/mainnet with TLS:

```bash wrap
# grpcurl with TLS
grpcurl \
  -d '{"address": "ansem1address..."}' \
  grpc.ansem.network:443 \
  cosmos.bank.v1beta1.Query/AllBalances
```

Go client with TLS:

```go wrap
import "google.golang.org/grpc/credentials"

creds := credentials.NewClientTLSFromCert(nil, "")
conn, err := grpc.Dial("grpc.ansem.network:443", grpc.WithTransportCredentials(creds))
```

## Error Handling

gRPC errors include status codes:

| Code | Name | Description |
|------|------|-------------|
| 0 | OK | Success |
| 3 | INVALID_ARGUMENT | Bad request parameters |
| 5 | NOT_FOUND | Resource not found |
| 13 | INTERNAL | Internal server error |
| 14 | UNAVAILABLE | Service unavailable |

Example error:
```json wrap
{
  "code": 5,
  "message": "rpc error: code = NotFound desc = account not found"
}
```

## Complete Service Reference

| Service | Purpose |
|---------|---------|
| `cosmos.auth.v1beta1.Query` | Account queries |
| `cosmos.bank.v1beta1.Query` | Balance queries |
| `cosmos.distribution.v1beta1.Query` | Rewards queries |
| `cosmos.gov.v1.Query` | Governance queries |
| `cosmos.mint.v1beta1.Query` | Minting queries |
| `cosmos.slashing.v1beta1.Query` | Slashing queries |
| `cosmos.staking.v1beta1.Query` | Staking queries |
| `cosmos.tx.v1beta1.Service` | Transaction service |
| `cosmos.base.tendermint.v1beta1.Service` | Tendermint queries |
| `cosmwasm.wasm.v1.Query` | CosmWasm queries |
